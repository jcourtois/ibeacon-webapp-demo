<h1>iBeacon Customer Registry</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @customers.each do |customer| %>
      <tr>
        <td><%= "#{customer.to_s}:" %></td>
        <td><%= link_to 'Visits', customer_visits_path(customer_id: customer[:id]) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Customer', new_customer_path %>
