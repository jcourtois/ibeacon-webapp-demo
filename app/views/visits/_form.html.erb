<%= form_for(@visit) do |f| %>
  <% if @visit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@visit.errors.count, "error") %> prohibited this visit from being saved:</h2>

      <ul>
      <% @visit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :enter_time %><br>
    <%= f.datetime_select :enter_time %>
  </div>
  <div class="field">
    <%= f.label :exit_time %><br>
    <%= f.datetime_select :exit_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
